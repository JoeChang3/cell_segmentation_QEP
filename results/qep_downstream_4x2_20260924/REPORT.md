# QEP × downstream 执行记录：环境阻塞

日期：2026-09-24（America/Phoenix）。

**状态：已执行预检查，尚未运行科学实验。新增训练 0 次，新增分割结果 0/16。**

## 已完成

- 主仓库检出冻结提交 `fa746853ba007552bb1d5a43340d91f6796096b1`。
- 原论文 R 参考仓库检出 `44714c2e0be958fe796a8fd4bdbc220dae3c23dd`。
- 两张 figure_1 开发图及其 GT 的 4 个 MD5 校验全部通过。
- 编写 4 种重建 × 2 种下游的驱动脚本，以及原 R 下游适配器和冻结 FastGP 参数恢复脚本。
- Python 语法检查通过；预检查实际执行，因依赖不满足退出，退出码为 2。
- 原有核心实现及 Rounds 1–10 结果未修改。没有重跑 q sweep 或评价其他图像。

## 当前阻塞

工作环境使用 Python 3.12.14，而冻结环境是 3.10.19；没有 Rscript、torch、gpytorch、qpytorch、scikit-image、imageio 和 tifffile，已有 numpy/pandas 版本也不同。

隔离 Conda 环境安装在下载依赖索引分片时收到代理返回的 HTTP 403（CONNECT tunnel failed），未能完成。这里没有可执行的真实 RobustGaSP/EBImage 环境。未用 Python 近似或不匹配依赖替代原论文方法。

这是运行条件阻塞，不是实验失败、QEP 负结果或新的科学结论。依据已批准方案第 5 节，不能把未通过兼容性与基准复现检查的数字用于比较。

## 待执行内容

在原研究环境或具有相同关键依赖的可执行环境中，按 `experiments/real_data/qep_downstream_4x2/README.md` 的命令运行。需要两份仓库和可用的 Python/R，不只是安装包版本清单。

执行顺序：依赖与文件检查 → 两张图的 Raw-P、FastGP-P、Raw-C 历史锚点 → 固定配置 QEP 重建及 QEP-C 历史锚点 → QEP-P → 完整 16 行及所有对照差值。任何锚点不符都会停止，不自动调整参数。

同一重建只生成一次，P/C 输入数组哈希必须一致。FastGP 使用 R 参考实现与已归档 R-side 参数；不会混用 Round 5 的 Python optimum。

已批准的范围仍然有效，无需再次批准这 16 个组合。继续执行所缺的是满足依赖的运行环境。

## 脚本验证边界

Python 驱动脚本仅通过语法检查及预检查路径验证。R 脚本、数值复现和完整运行仍未验证；不能称为已验证完成的实验软件。每个 QEP tile 的诊断与数组会保存，但尚未实现跨运行缓存接续。失败后需使用新输出目录。

预先工程门槛保留：AP50 对原方法提升 ≥0.02，且对同下游最强 Raw/FastGP/Q2 控制提升 ≥0.01。AP75 与新增 split 的保护条件在驱动脚本中保守地对所有列出的控制检查，所有差值均保存。门槛不是统计显著性。

不能从此记录得出 QEP 优于或劣于 FastGP 的新结论。
